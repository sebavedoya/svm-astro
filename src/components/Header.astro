---
const links = [
  { href: '/sistema', label: 'Sistema' },
  { href: '/proyectos', label: 'Proyectos' },
  { href: '/reflexiones', label: 'Reflexiones' },
  { href: '/prensa', label: 'Prensa' },
  { href: '/contacto', label: 'Contacto' },
];
---
<header class="fixed top-0 left-0 w-full backdrop-blur-md bg-black/40 px-6 md:px-10 py-4 z-50">
  <div class="max-w-6xl mx-auto flex items-center justify-between">
    <a href="/" class="font-heading tracking-wide">Sebastián Vedoya M.</a>
    <nav class="hidden md:flex gap-6 text-text-dim text-sm">
      {links.map(l => <a class="hover:text-accent-alt transition-colors" href={l.href}>{l.label}</a>)}
    </nav>
  </div>
</header>
