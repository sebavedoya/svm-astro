---
import Default from '../layouts/DefaultLayout.astro';
import Hero from '../components/Hero.tsx';

const processSteps = [
  {
    titulo: 'Diagnóstico',
    detalle: 'Audiencias, contexto, riesgos y oportunidades para definir las coordenadas estratégicas.',
  },
  {
    titulo: 'Diseño',
    detalle: 'Mensajes, narrativa, mix de canales y calendarización con gobernanza clara.',
  },
  {
    titulo: 'Ejecución',
    detalle: 'Coordinación de equipos, piezas, prensa, RRSS y alianzas en despliegues 360°.',
  },
  {
    titulo: 'Medición',
    detalle: 'KPIs, aprendizajes y ajustes para iterar, escalar y estandarizar procesos.',
  },
];

const quickLinks = [
  {
    href: '/acerca',
    titulo: 'Acerca de mí',
    texto: 'Conoce la trayectoria y la manera de trabajar con organizaciones públicas y privadas.',
  },
  {
    href: '/proyectos',
    titulo: 'Casos de estudio',
    texto: 'Revisa proyectos con resultados concretos en posicionamiento, campañas y prensa.',
  },
  {
    href: '/servicios',
    titulo: 'Servicios',
    texto: 'Planes estratégicos, narrativas sectoriales, prensa y workshops para equipos.',
  },
];

const blogPreview = [
  {
    titulo: 'La Ley de la Variedad Requerida y su valor para diseñar estrategia comunicacional hoy',
    resumen: 'Marco de Ashby aplicado a entornos hiperfragmentados y matrices de decisión.',
  },
  {
    titulo: 'Crisis comunicacional en tiempos de hiperconexión: claves y errores frecuentes',
    resumen: 'Principios de preparación, portavocía y control de daños con foco en aprendizaje continuo.',
  },
  {
    titulo: 'Comunicar sin miedo',
    resumen: 'Notas para simplificar procesos y sostener el tono institucional en escenarios tensos.',
  },
];
---
<Default title="Inicio">
  <Hero client:load />

  <section class="mx-auto mt-16 max-w-6xl px-6">
    <div class="rounded-[2.5rem] border border-black/5 bg-[#F0E8D9] p-10 shadow-[0_26px_52px_rgba(0,0,0,0.12)]">
      <div class="flex flex-col gap-6 md:flex-row md:items-end md:justify-between">
        <div>
          <p class="text-xs uppercase tracking-[0.35em] text-text-dim">Proceso estratégico</p>
          <h2 class="mt-4 text-3xl font-heading tracking-[-0.02em] text-text-light">
            Diagnóstico → Diseño → Ejecución → Medición
          </h2>
        </div>
        <p class="max-w-xl text-sm font-medium text-text-dim">
          Cada fase combina análisis, narrativa y conducción de equipos para responder a contextos complejos. Los entregables están
          diseñados para integrar datos, audiencias y posicionamiento institucional.
        </p>
      </div>
      <div class="mt-10 grid gap-4 md:grid-cols-4">
        {processSteps.map(({ titulo, detalle }, index) => (
          <article class="rounded-2xl border border-black/10 bg-white/90 p-6 shadow-[0_16px_32px_rgba(0,0,0,0.08)]">
            <p class="text-xs font-medium uppercase tracking-[0.3em] text-text-dim">Fase 0{index + 1}</p>
            <h3 class="mt-4 text-lg font-heading tracking-[-0.01em] text-text-light">{titulo}</h3>
            <p class="mt-3 text-sm text-text-dim">{detalle}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="mx-auto max-w-6xl px-6 py-16">
    <div class="grid gap-8 md:grid-cols-3">
      {quickLinks.map(({ href, titulo, texto }) => (
        <a href={href} class="group flex h-full flex-col gap-4 rounded-[2rem] border border-black/10 bg-bg-card p-8 shadow-[0_24px_48px_rgba(0,0,0,0.1)] transition hover:-translate-y-2 hover:shadow-[0_32px_64px_rgba(0,0,0,0.15)]">
          <h3 class="text-2xl font-heading tracking-[-0.015em] text-text-light group-hover:text-accent">{titulo}</h3>
          <p class="text-sm font-medium text-text-dim">{texto}</p>
          <span class="mt-auto text-sm font-semibold text-accent">Explorar →</span>
        </a>
      ))}
    </div>
  </section>

  <section class="bg-[#141414] py-16">
    <div class="mx-auto flex max-w-5xl flex-col gap-6 px-6 text-white">
      <div class="flex flex-col gap-3 md:flex-row md:items-end md:justify-between">
        <h2 class="text-3xl font-heading tracking-[-0.02em]">Blog / Ideas / Lecturas</h2>
        <a href="/blog" class="text-sm font-semibold text-accent-alt">Ver más ideas →</a>
      </div>
      <div class="grid gap-6 md:grid-cols-3">
        {blogPreview.map(({ titulo, resumen }) => (
          <article class="rounded-2xl border border-white/10 bg-white/10 p-6 backdrop-blur-sm">
            <h3 class="text-lg font-heading tracking-[-0.01em] text-white">{titulo}</h3>
            <p class="mt-3 text-sm text-white/70">{resumen}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="mx-auto max-w-4xl px-6 py-16 text-center">
    <h2 class="text-3xl font-heading tracking-[-0.02em] text-text-light">¿Tienes un desafío comunicacional?</h2>
    <p class="mt-4 text-lg font-medium text-text-dim">
      Conversemos sobre cómo alinear estrategia, narrativa y datos para fortalecer tu institución.
    </p>
    <a href="/contacto" class="mt-8 inline-flex items-center justify-center rounded-full bg-accent px-8 py-3 font-semibold text-white transition hover:bg-accent-alt hover:text-text-light">
      Contáctame
    </a>
  </section>
</Default>
